Магазин продуктов. 
	грузчик 
	 * загружает товар
	 * разгружает товар 
	 * получает накладную
	продавец
	 * принимает товар
	 * сортирует товар
	 * разставляет товар по полкам
	 * выписывает накладную 
	кассир
	 * пробивает товар
	 * упаковывает товар
	 * принимает оплату от посетителей
	посетитель 
	 * выбирает товар
	 * складывает в тележку
	 * передает оплату
	проверяющий
	 * просматривает полки
	 * проверяет накладную
	 * опрашиает посетителя
	 * проверяет доход 
