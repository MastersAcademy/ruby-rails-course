### Start app via terminal on local machine
Use bundler command to install all needed gems
```sh
$ bundler install
```
To start sinatra
```sh
$ rake start
```
Go to [localhost](http://localhost:4567/) to test app
