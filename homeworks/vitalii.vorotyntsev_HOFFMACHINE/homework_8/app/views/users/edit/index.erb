<div class="ui dividing header">Edit user</div>

<div class="ui form">
  <form id="edit_user-form" action="/users/<%= @user.id %>" method="post" novalidate>
    <input type="hidden" name="_method" value="patch">

    <div class="three fields">
      <div class="required field">
        <label>Email</label>
        <input type="email" name="email" value="<%= @user.email %>">
      </div>
      <div class="required field">
        <label>Last Name</label>
        <input type="text" name="first_name" value="<%= @user.first_name %>">
      </div>
      <div class="required field">
        <label>First Name</label>
        <input type="text" name="last_name" value="<%= @user.last_name %>">
      </div>
    </div>
    <div class="field">
      <label>About</label>
      <textarea name="about"><%= @user.about %></textarea>
    </div>

    <div class="actions">
      <button class="ui green button" type="submit">Save</button>
      <a class="ui button" href="/users">Cancel</a>
    </div>
  </form>
</div>
